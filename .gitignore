node_modules/

# Local append-only ledger data
*.db
*.db-shm
*.db-wal

# Runtime logs and local environment
*.log
.env
.env.*
!.env.example

# Deployment/tooling caches
.wrangler/

# Never publish copied secret notes.
*Token*.txt

# Build output
dist/

# Local agent registry; commit only the example template.
config/agents.json

# Local schedule registry; commit only the example template.
config/schedules.json

# Local update artifacts; not part of the published source.
_backup-v0.3.1/
ai-board-main-v1.0.0-rc.1.zip
